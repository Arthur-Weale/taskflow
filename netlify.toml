[build]
  base = "TaskFlow.Web"
  publish = "publish/wwwroot"
  command = "curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --version 9.0.0 && export PATH=$PATH:$HOME/.dotnet && dotnet publish -c Release -o publish"

[build.environment]
  DOTNET_VERSION = "9.0"