[build]
  base = "TaskFlow.Web"
  publish = "publish/wwwroot"
  command = "curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --channel 9.0 --install-dir $HOME/.dotnet9 && $HOME/.dotnet9/dotnet publish -c Release -o publish"

[build.environment]
  DOTNET_VERSION = "9.0"